// NoteDeck ai.json5 デフォルト
// AI 設定（API キー / endpoint は含まない — Vault 接続 (#564) に統合済み）
{
  // 使用する Vault 接続の id。空 = 未選択 (AI 設定で接続を選ぶ)。
  // endpoint / API キー / protocol はこの接続から解決する。
  activeConnectionId: '',
  // 接続ごとのモデル名 { [connectionId]: model }。
  models: {},

  // 権限 (誰に何を許すか) は principal 別の permissions.json5 に移行した (#712)。
  // ai.json5 は AI 固有の設定 (接続 / モデル / dataSources / HEARTBEAT スケジュール)
  // だけを持つ。

  // データソース: AI に送る system prompt に含める情報
  // permissions と違い、Phase 1 から即動作する (system prompt 末尾に <notedeck-context> として注入)
  // - memos: ローカル Zettelkasten 形式の markdown メモを <memos> ブロックとして
  //   注入。active account のみ、updatedAt 降順で上限 20 件。
  dataSources: {
    preset: 'readonly',
    custom: {
      'currentAccount': true,
      'currentColumn': true,
      'visibleNotes': false,
      'recentConversation': false,
      'memos': true,
    },
  },

  // HEARTBEAT (Phase 6, #411): AI が定期的に自律起動して未読 / 重要事項を拾う
  // OpenClaw の HEARTBEAT.md 仕様準拠 — Tauri アプリ起動中は常時動作する
  // global daemon。AI カラムの有無に関係なく走る。
  // - enabled: false がデフォルト。明示 ON で初めて daemon が動く
  // - intervalMinutes: 1 〜 1440 (24h)。短いほど API コスト増大に注意
  // - どの skill を heartbeat 対象にするかは skill 側 frontmatter (`mode: heartbeat`)
  //   スキルカラムから個別 toggle 可能
  // - target: tick 結果の出力先 AI session
  //     'auto' = kind='heartbeat' の専用 session を auto-create + 永続使用 (default)
  //     'none' = session に append しない (silent log only)
  //     <session id> = 既存 session に明示 pin
  // - HEARTBEAT 中の権限は permissions.json5 の 'ai.heartbeat' で独立管理 (#712)

  // Persona (#491): この AI が振る舞う人格を `isPersona: true` な skill から選ぶ。
  // 未指定 = 汎用 AI。AI 設定ウィンドウで切替可能。chat / heartbeat / command /
  // task すべての session に同じ persona が反映される (= 同一性設定)。
  personaSkillId: '',

  heartbeat: {
    enabled: false,
    intervalMinutes: 30,
    target: 'auto',
    // Cheap Check First (#411): tick 開始時に「変化検知」用の軽量
    // capability (cheap=true) を呼び、前回値と一致すれば AI を skip
    // して HEARTBEAT_OK 扱いにする。skill 側で
    // `cheapCheckCapabilities: [...]` を frontmatter で宣言した skill
    // にだけ発動する (= opt-in)。
    // - enabled: false なら全 skill で機構を停止 (= 常に AI を叩く)
    // - maxSkipHours: 変化なしでも N 時間に 1 回は強制 AI 起動
    //   (cheap check が壊れていた場合のセーフティ)
    cheapCheck: {
      enabled: true,
      maxSkipHours: 24,
    },
    // 1 日あたりの AI 起動上限。Cheap Check で skip された tick は
    // カウントされない。デフォルト 48 = 30 分 interval で毎回叩いた
    // 場合の最大値。
    // onDailyLimit:
    //   'warn'    = toast 出して継続 (= AI 呼んで進める、default)
    //   'disable' = toast + heartbeat.enabled=false で daemon 停止
    dailyMaxAiRuns: 48,
    onDailyLimit: 'warn',
    // HEARTBEAT が「重要発見」と判定した内容を OS デスクトップ通知として
    // 表示するか。target='none' (silent log) のときは出さない。
    // アプリにフォーカスがあるときも自動抑制 (sendDesktopNotification 内で
    // document.hasFocus() を判定)。
    desktopNotification: true,
  },

  // 生成まわりの調整値。既定のまま使えることを前提に、実行先のモデルに
  // よって既定が合わなくなるものだけを開けている。範囲外の値は読み込み時に
  // clamp される (useAiConfig の normalizeGenerationConfig)。
  generation: {
    // 応答の最大トークン。0 = プロバイダーの既定に任せる。
    // Anthropic は max_tokens 必須なので、0 でもこの既定値を送る。
    maxTokens: 4096,
    // tool 呼び出しの上限ラウンド。1 ラウンド = AI が tool を呼び、結果を
    // 渡して再度応答するまで。上げるほど 1 回の依頼で到達できる作業が
    // 増える一方、費用と暴走時の被害も比例して増える。
    maxToolRounds: 10,
    // セッションのタイトル生成に使う最大トークン。この上限は thinking を
    // 含む出力全体にかかる一方、タイトルとして拾うのは本文だけなので、
    // reasoning 系モデルでは思考の取り分を見込んで広く取る。
    titleMaxTokens: 512,
    // 応答待ちのアイドルタイムアウト (秒)。バイトが届かなくなってからの
    // 計測なので健全な長考は切らない。ローカル LLM のように最初の
    // トークンまで待つ実行先では伸ばす。
    readTimeoutSeconds: 120,
  },
}
